<?xml version="1.0" encoding="utf-8"?>
<!--
  Layout de la pantalla "Añadir medicamento".
  - Fondo: Preview de cámara a pantalla completa.
  - Header superpuesto: linterna (izq) + salir (der).
  - Texto central inferior con instrucciones/estado.
  - Botón grande "Detectar" en la parte inferior.
-->

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".AddDrugActivity">

    <!-- =========================================================
         CÁMARA (fondo): vista previa a pantalla completa
         ========================================================= -->
    <androidx.camera.view.PreviewView
        android:id="@+id/viewFinder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="Vista previa de la cámara para agregar un medicamento" />

    <!-- =========================================================
         HEADER superpuesto (linterna izquierda / salir derecha)
         ========================================================= -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="130dp"
        android:layout_margin="10dp"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Linterna -->
        <ImageButton
            android:id="@+id/flashButton"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginEnd="10dp"
            android:alpha="0.8"
            android:background="@drawable/ripple_round_button"
            android:contentDescription="Activar o desactivar linterna"
            android:padding="30dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_lantern_off"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintWidth_percent="0.32" />

        <!-- Botón Salir -->
        <ImageButton
            android:id="@+id/exitButton"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_marginStart="10dp"
            android:alpha="0.8"
            android:background="@drawable/ripple_round_button"
            android:contentDescription="Salir y volver a pantalla de detección."
            android:padding="30dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_exit"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintDimensionRatio="1:1"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintWidth_percent="0.32" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- =========================================================
         TEXTO: instrucciones / resultado (superpuesto, centrado)
         ========================================================= -->
    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="75dp"
        android:background="#10DDDDDD"
        android:contentDescription="Instrucciones"
        android:gravity="center"
        android:padding="10dp"
        android:text="@string/texto_instrucciones"
        android:textColor="#80FFFFFF"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.5" />

    <!-- =========================================================
         BOTÓN DETECTAR (contenedor con fondo redondeado)
         ========================================================= -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginStart="10dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp"
        android:background="@drawable/fondo_redondeado"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/textView">

        <Button
            android:id="@+id/detection_button"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/button_3d"
            android:backgroundTint="@null"
            android:contentDescription="Botón para iniciar detección de medicamento"
            android:elevation="8dp"
            android:foreground="?attr/selectableItemBackground"
            android:gravity="center"
            android:text="@string/detection"
            android:textColor="@android:color/white"
            android:textSize="60sp"
            android:textStyle="bold"
            android:tint="@null"
            app:backgroundTint="@null" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
